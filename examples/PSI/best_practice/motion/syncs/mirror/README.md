# Mirror Sync Example

This directory contains the runnable mirror synchronization example.

- Startup file: `startup.script`
- PLC file: `cfg/main.plc`
- Axis and hardware files: `cfg/*.yaml`, `cfg/hw.subst`

Run it from this directory:

```sh
iocsh.bash startup.script
```
