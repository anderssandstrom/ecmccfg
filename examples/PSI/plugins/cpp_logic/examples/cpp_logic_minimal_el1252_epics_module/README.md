# `MTEST04-MTN-CPP-LOGIC`

This IOC example is the loading a CPP logic as a module instead of the "normal" when included in IOC.

The module sources are in the "module" subdir and needs to be installaed like any other EPICS module (tagged and then make install).


